# SimpleSpringBootApp
Simple Spring Boot App which manages a cocktail bar.

The DBMS used is Postgres SQL. You start it using the Dockerfile.

The use cases are:

-Adding a cocktail (name, id).
-Update a cocktail based on its id.
-Deletion of a cocktail based on its id.
-Display of all available cocktails.
